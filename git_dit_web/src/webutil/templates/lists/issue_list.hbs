{{> header}}
<body>

{{> navbar}}

<div class="container-fluid text-center">    
  <div class="row content">

    <div class="col-sm-2 sidenav">
    {{> sidenav}}
    </div>
    <div class="col-sm-4 text-left">
    {{> submitIssue}}

    {{#each issues}}   
      <ul>
        {{#each messages}}
          <div class="row">
            <div class="col-*-*"><a href="/show_issue/{{hash_id}}/newest_first">
                {{author.name}}
                {{author.email}}
                {{author.when}}
                </a>
            </div>
          </div>
          {{/each}}
      </ul>
    {{/each}}
    </div>
  </div>
</div>
</body>